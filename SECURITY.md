# سياسة الأمان — Security Policy

هذا المستند يوضّح نموذج التهديد لمواقع هذا المستودع، وما هو محمي فعلاً وما ليس كذلك،
وكيفية الإبلاغ عن ثغرة.

---

## المواقع في هذا المستودع

| الموقع | المسار | الاستضافة | البيانات |
|--------|--------|-----------|----------|
| **Elwaset** (البورتفوليو) | جذر المستودع | GitHub Pages | Firebase Firestore (تقييمات + بيانات عملاء) |
| **TechAI News** | `techai/` | GitHub Pages | `localStorage` في متصفح الزائر فقط |

---

## ما هو محمي فعلاً

### البورتفوليو (Elwaset)

- **قواعد أمان Firestore** (`firestore.rules`) — هذا هو خط الدفاع الحقيقي:
  - `customers` (تحتوي أسماء وبريد وأرقام هواتف): **القراءة ممنوعة تماماً من العميل**.
    الاطلاع عليها من Firebase Console فقط.
  - `reviews`: تُقرأ فقط إذا كانت `approved == true`. أي زائر يستطيع إرسال تقييم،
    لكن بحقول مقيّدة النوع والطول، و`approved` مثبّتة على `false` — فلا يستطيع أحد
    نشر تقييم معتمد من تلقاء نفسه.
  - التعديل والحذف ممنوعان من العميل في كل المجموعات.
  - قاعدة افتراضية ترفض كل ما عداها.
- **تهريب المخرجات (XSS)**: كل قيمة قادمة من المستخدم (الاسم، البريد، الهاتف، نص
  التقييم، صورة البروفايل) تُهرَّب قبل حقنها في الصفحة. صور البروفايل تُقبل من
  `https:` فقط — يمنع `javascript:` و`data:` والهروب من السمة.
- **حدود الإدخال**: الاسم 60، الهاتف 20 (بنمط محدد)، نص التقييم 1000 محرف،
  والتقييم مقيّد بين 1 و5 — على العميل وفي قواعد Firestore معاً.
- **CSP** عبر `<meta>` على كل الصفحات، يقصر السكربتات والأنماط والاتصالات على
  المصادر المعروفة (Firebase، Google Fonts، cdnjs).
- **SRI** على ملف Font Awesome من cdnjs، مع `crossorigin` و`referrerpolicy`.
- **`rel="noopener noreferrer"`** على كل الروابط الخارجية.
- **`referrer` policy**: `strict-origin-when-cross-origin`.

### TechAI News

- تهريب شامل للمخرجات، وقوائم بيضاء لأسماء الأصناف ومعرّفات يوتيوب.
- كلمات المرور تُخزَّن كبصمة SHA-256 مع مِلح فريد لكل مستخدم (Web Crypto).
- قفل لوحة التحكم بكلمة مرور، بمهلة خمول 30 دقيقة، وتأخير تصاعدي بعد المحاولات
  الفاشلة، وأدوار (مدير/محرر/مراجع)، وسجل عمليات.
- حدود إدخال وتبريد بين التعليقات.

---

## ما هو **غير** محمي — اقرأ هذا

### 1. مفتاح Firebase في `firebase-config.js` عام بالتصميم

هذا ليس تسريباً. مفاتيح Firebase للويب مصمّمة لتكون مرئية في المتصفح.
**الشيء الوحيد الذي يحمي بياناتك هو `firestore.rules`.**

**إجراء مطلوب منك:**
1. انشر القواعد: `firebase deploy --only firestore:rules`
   أو من Firebase Console ← Firestore ← Rules ← الصق محتوى `firestore.rules`.
2. من Google Cloud Console ← Credentials، قيّد مفتاح الـ API بـ HTTP referrers
   على نطاقاتك فقط (`elwaset.net`، `*.elwaset.net`).
3. تأكد أن القواعد ليست في وضع الاختبار المفتوح (`allow read, write: if true`).

### 2. لوحة تحكم TechAI قفل واجهة فقط

الموقع static بلا خادم. من يملك الجهاز يستطيع تجاوز القفل من أدوات المطوّر
وتعديل `localStorage` مباشرة. الغرض من القفل منع العبث العابر وتوثيق العمليات،
**لا الحماية من مهاجم جاد**. الحماية الفعلية تتطلب باك إند يتحقق من الصلاحيات
على الخادم (Firebase Auth + Custom Claims، أو API).

### 3. النقاط والمسابقات قابلة للتلاعب

نظام النقاط موقّع ببصمة تكشف التعديل العابر وتُرجع القيمة، لكنه **رادع لا حماية**.
أي جائزة حقيقية يجب التحقق منها على الخادم قبل الصرف.

### 4. رؤوس HTTP غير متاحة على GitHub Pages

GitHub Pages لا يسمح بضبط رؤوس استجابة مخصصة، لذا:
- `frame-ancestors` (منع clickjacking) **غير مفعّل** — لا يعمل عبر `<meta>`.
- `X-Content-Type-Options` و`Strict-Transport-Security` غير مضبوطة.

للحصول عليها، انقل الاستضافة إلى Cloudflare Pages أو Netlify أو Vercel وأضف
ملف رؤوس (`_headers` أو `netlify.toml` أو `vercel.json`).

---

## ملاحظات على `bot_dev/` (لم تُعدَّل)

عند نشر البوت، انتبه لأمرين وردا في التوثيق الحالي:

- `bot_dev/CONNECTIVITY_TROUBLESHOOT.md` يقترح تفعيل `PasswordAuthentication yes`
  لـ SSH كحل لمشكلة اتصال. هذا يضعف الأمان بشكل ملموس — يُفضّل إصلاح الوصول
  بالمفاتيح بدل تفعيل الدخول بكلمة مرور.
- `bot_dev/DEPLOYMENT_GUIDE.md` يذكر عنوان IP لخادم صراحةً. ليس سراً بحد ذاته،
  لكن إخفاؤه من التوثيق العام يقلّل سطح الاستطلاع.

توكن البوت يُقرأ من متغير بيئة ولا يوجد أي توكن حقيقي في المستودع — هذا صحيح.

---

## الإبلاغ عن ثغرة

إذا اكتشفت ثغرة، راسلنا على **elawadi.store4@gmail.com** بعنوان يبدأ بـ `[Security]`.

أرجو ألا تفتح Issue عاماً بتفاصيل الثغرة قبل إصلاحها. نرد عادة خلال 48 ساعة.

**نطاق الاهتمام**: تنفيذ سكربتات (XSS)، تجاوز قواعد Firestore، كشف بيانات
العملاء، تسريب بيانات شخصية.

**خارج النطاق**: تعديل المستخدم لبيانات `localStorage` الخاصة به (سلوك متوقع
في موقع static)، ووجود مفتاح Firebase العام في الكود (بالتصميم).
