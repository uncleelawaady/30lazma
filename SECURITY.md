# الأمان — قواعد الأسرار في الريبو ده

**الريبو ده عام.** أي حاجة تتكتب هنا يقدر أي حد يقراها، ولو اتمسحت بعدين بتفضل موجودة في تاريخ الجيت.

---

## القاعدة

| النوع | فين يتحط |
|---|---|
| Hostinger API token | متغير بيئة `HOSTINGER_API_TOKEN` على الجهاز |
| Telegram bot token | ملف `.env` (متجاهَل في `.gitignore`) |
| Firebase Service Account / Admin SDK | مايتحطش في الريبو ولا في أي ملف بيوصل للمتصفح خالص |
| Firebase Web config (`apiKey` وإخواته) | عادي في الريبو — ده **مش سر**، الحماية في `firestore.rules` |

الفرق في آخر سطرين مهم: مفتاح الويب بتاع Firebase معمول عشان يتنشر، ومبيدّيش صلاحية لوحده. الـService Account بيدّي صلاحية كاملة على قاعدة البيانات ويتخطى كل القواعد.

---

## لو اتسرب سر

**١. Revoke الأول.** قبل أي حاجة تانية. تنضيف الملف أو الـcommit **مش** بيبطّل السر — بيخفيه بس.

**٢. اعمل واحد جديد** وحطه في متغير بيئة. متبعتوش في شات ومتحطهوش في لقطة شاشة ومتكتبهوش في ملف داخل الريبو.

**٣. نضّف التاريخ** — بعد الـRevoke. لاحظ إن GitHub بيفضل يقدّم الـcommit القديم من الـSHA بتاعه حتى بعد الـforce-push، وأي نسخة (fork/clone) اتاخدت قبل كده بتفضل معاها السر.

---

## حالة معروفة

Hostinger API token كان مكتوب صريح في `.claude/settings.json` من أول commit في الريبو. الملف اتنضّف وبقى بيقرا من متغير بيئة، والتوكن **اتعتبر محروق ولازم يتعمله Revoke** — التاريخ لسه فيه القيمة القديمة لحد ما يتنضّف.

---

## قبل أي commit

```bash
git diff --cached | grep -inE 'token|secret|api[_-]?key|password|BEGIN .*PRIVATE KEY'
```

لو طلع أي سطر فيه قيمة حقيقية — اوقف ونضّفه قبل ما تعمل commit.
